# Changelog

One line per shipped thing. `[external]` tags mark approved outward-facing actions.

- 2026-09-20 — Slice 1 walking skeleton scaffolded: MapMoment Xcode project (SwiftUI/MapKit/CoreLocation + Firebase SPM), Sign in with Apple → Firebase Auth flow, live group-map w/ Firestore listener, significant-change location publisher, FCM push registration, `sendTestPing` Cloud Function, Firestore rules, mock mode for the Simulator, SETUP.md. Verified: xcodegen + Swift parse + function loads; NOT compiled/run (no Xcode on this Mac — see open-loops).
