# Roadmap — Friends App

> Checklist instance of the hub's `context/roadmap.md`. Check items off as they're genuinely done (evidence, not intention). Gates require every item above them. Skipping a gate = Elliott override logged in `decisions.md`.
>
> Note: this venture predates the roadmap. Its smallest test IS a built TestFlight beta, so the ship date (2026-10-20) was set during validating — building overlaps validating by design here; the beta metrics are the validation gate.

## 1 · Spark

- [x] Interview captured: one-liner, ICP, alternative, revenue guess, platform
- [x] Workspace scaffolded + registered (with `validate_by` date)
- [x] Assumption map scored (desirability / viability / feasibility)
- [x] Competitor scan (≥5 real competitors or sourced "few exist")
- [x] Demand check with cited sources

**Gate → validating:** riskiest assumption + smallest test + pre-committed pass/fail threshold in `venture.md`
- [x] Gate passed (date: 2026-09-20)

## 2 · Validating

- [ ] Riskiest-assumption test running (TestFlight beta live with friend group)
- [ ] ≥5 Mom Test interviews with real ICP members (the friend group — how they use Find My today)
- [ ] Positioning drafted from interview evidence
- [ ] Midpoint kill/reshape checkpoint
- [ ] Test result vs threshold (week-4 numbers), written to `metrics.md`

**Gate → building:** test passed at threshold · positioning filled · one-metric chosen · **ship deadline set now**
- [ ] Gate passed (date: )

## 3 · Building (overlaps validating for this venture — the beta is the test)

- [ ] `mvp-scope` PRD (live map + invisibility windows + spying ping only)
- [ ] MVP built on confirmed stack (SwiftUI + MapKit + CoreLocation)
- [ ] Beta metrics instrumented (Always-grant rate, sharing-on rate, opens/week) + error visibility
- [ ] Landing page (per `context/design-workflow.md`) — if needed beyond TestFlight invite
- [ ] Launch plan drafted (`launch-playbook`) — post-beta, only if validation passes

**Gate → launched:** `ship-checklist` pass · `panel-review` SHIP · launch drafts approved
- [ ] Gate passed (date: )

## 4 · Launched

- [ ] Launch executed, same-day numbers in `metrics.md`
- [ ] Directory sweep queued (`context/launch-directories.md`)
- [ ] 48h retro with per-channel KEEP/KILL
- [ ] Weekly leverage call scheduled
